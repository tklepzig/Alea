// Single source of truth for the app's identity. "Arca" is a working title —
// a rename only touches this file, manifest.webmanifest, index.html
// (title + wordmark) and the icon SVGs.

/** Lowercase id: localStorage key prefix + service-worker cache name. */
export const APP_ID = "arca";
/** Display name (document title, hub wordmark alt text). */
export const APP_NAME = "Arca";
